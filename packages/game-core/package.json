{
  "name": "@game/core",
  "version": "0.1.0",
  "private": true,
  "description": "Lógica pura do jogo (combate, progressão idle) — sem dependência de UI",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "simulate": "tsx scripts/simulate.ts"
  },
  "dependencies": {
    "@game/data": "*"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.23.12",
    "typescript": "^5",
    "vitest": "^3"
  }
}
